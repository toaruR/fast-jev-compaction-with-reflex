{
  "name": "fast-jev-compaction-with-reflex",
  "version": "0.3.0",
  "description": "Verbatim Jev-guided compaction for Claude Code sessions.",
  "license": "MIT",
  "userConfig": {
    "apiKey": {
      "type": "string",
      "title": "Reflex API key",
      "description": "Bearer key for a reflex-serve daemon started with --api-key. Leave unset for a local, unauthenticated server, or to use REFLEX_API_KEY.",
      "sensitive": true
    },
    "keepThreshold": {
      "type": "number",
      "title": "Keep threshold",
      "description": "Minimum reflex probability that a tool call or tool result is still needed; below it the item is removed.",
      "default": 0.5
    },
    "preserveRecentMessages": {
      "type": "number",
      "title": "Recent messages to preserve",
      "description": "Number of newest messages pinned from compaction.",
      "default": 6
    },
    "compactAtPercent": {
      "type": "number",
      "title": "Compaction percentage",
      "description": "Context percentage at which turn.complete requests compaction.",
      "default": 60
    },
    "minReductionRatio": {
      "type": "number",
      "title": "Minimum reduction ratio",
      "description": "Minimum estimated character reduction required to replace history.",
      "default": 0.25
    },
    "maxStateTokens": {
      "type": "number",
      "title": "Maximum reflex state tokens",
      "description": "Estimated token budget for the conversation state sent with every reflex request. The state's KV cache is copied per question branch on the GPU, so this, not the question count, decides a 529 OOM; 8000 fits a 12GB GPU with reflex-serve --max-pack-tokens 192 --state-cache-entries 2. Longer histories fall back to the built-in summary.",
      "default": 8000
    },
    "maxRequestTokens": {
      "type": "number",
      "title": "Maximum reflex request tokens",
      "description": "Estimated token budget for state plus questions in one reflex request; the questions are split across as many requests as this requires.",
      "default": 10000
    },
    "truncateHeadChars": {
      "type": "number",
      "title": "Truncated tool result head",
      "description": "Characters of a tool result kept when reflex decides its full output is no longer needed; 0 keeps only the note.",
      "default": 300
    },
    "model": {
      "type": "string",
      "title": "Reflex model",
      "description": "Model name reported to the reflex-serve daemon (informational only; the daemon serves whatever it was started with).",
      "default": "Qwen/Qwen3.5-4B"
    }
  }
}
